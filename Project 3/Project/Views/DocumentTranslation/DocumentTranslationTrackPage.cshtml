@model Project.Models.DocumentTranslationModel[]

@{
    ViewData["Title"] = "Document Translation Tracking";
}

<!DOCTYPE html>

<h2>Document Translation Tracking</h2>

<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.First().CustomerId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().FullName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().Address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().MobileNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().OriginalLanguage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().TranslatedLanguage)
        </th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MobileNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OriginalLanguage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TranslatedLanguage)
            </td>

            <td>
                <a href="@Url.Action("DocumentTranslationShowPage", "DocumentTranslation", new { id = item.DocumentTranslationId })">Show</a>
                <a href="@Url.Action("Delete", "DocumentTranslation", new { id = item.DocumentTranslationId })" class="delete-link" onclick="return confirmDelete()">Delete</a>
            </td>
        </tr>
    }
    </tbody>
</table>

@section scripts {
    <script>
        $(document).ready(function () {
            // Iterate over each canvas with the 'displayCanvas' class
            $('.displayCanvas').each(function () {
                var canvas = this;
                var signaturePad = new SignaturePad(canvas);

                // Function to load and display the signature
                function loadAndDisplaySignature(signatureData) {
                    signaturePad.fromDataURL(signatureData);
                }

                // Call the function with the signature data from the canvas's data attribute
                console.log("display:" + $(canvas).data('signaturestring'));
                loadAndDisplaySignature($(canvas).data('signaturestring'));
            });
        });
        
        function confirmDelete() {
                return confirm("Are you sure you want to delete this item?");
        }
    </script>
}